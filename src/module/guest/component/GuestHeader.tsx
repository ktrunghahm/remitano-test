import { AppBar } from "@mui/material";
import * as React from "react";

interface IGuestHeaderProps {}

const GuestHeader = (props: IGuestHeaderProps) => {
  return <AppBar></AppBar>;
};

export default GuestHeader;
